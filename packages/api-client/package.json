{
  "name": "@jc/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Client HTTP typé de l'API Jean-Claude — partagé web, mobile et desktop",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "react-native": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "typecheck": "tsc -b --dry"
  },
  "dependencies": {
    "@jc/domain": "*"
  }
}
