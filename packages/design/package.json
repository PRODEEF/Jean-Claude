{
  "name": "@jc/design",
  "version": "0.1.0",
  "private": true,
  "description": "Jetons de design partagés — couleurs, espacements, typographie, rayons",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "react-native": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "typecheck": "tsc -b --dry"
  }
}
